{
  "name": "@foundryspec/monorepo",
  "private": true,
  "description": "Monorepo for FoundrySpec",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "author": "FutureFoundAI",
  "license": "AGPL-3.0-only"
}
